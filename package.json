{
  "name": "promptforge",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter @promptforge/shared build && bun --filter \"!@promptforge/shared\" build"
  }
}
